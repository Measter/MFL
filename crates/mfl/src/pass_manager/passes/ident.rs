use std::ops::ControlFlow;

use crate::{context::Context, pass_manager::PassResult, Stores};

pub fn resolve_signatures(ctx: &mut Context, stores: &mut Stores) -> ControlFlow<(), PassResult> {
    todo!()
}
